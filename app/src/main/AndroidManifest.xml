<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.overdose.homeschooljoint">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CourseActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".activity.HomeWorkStudentActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.HomeWorkTeacherActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.LeaveActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.RatifyActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.ScoreActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.ModifyPWActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.UserCenterActivity"
            android:launchMode="singleTop" />
    </application>

</manifest>